{
  "name": "@ocp-catalog/ocp-webmcp-mcp-demo-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bunx vite --host 0.0.0.0 --port 4250",
    "start": "bunx vite preview --host 0.0.0.0 --port 4250",
    "build": "bunx vite build",
    "preview": "bunx vite preview --host 0.0.0.0 --port 4250",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "bun test --pass-with-no-tests"
  },
  "dependencies": {
    "@ocp-catalog/webmcp-adapter": "workspace:*",
    "react": "^19.1.1",
    "react-dom": "^19.1.1"
  },
  "devDependencies": {
    "@types/react": "^19.1.12",
    "@types/react-dom": "^19.1.9",
    "@vitejs/plugin-react": "^6.0.1",
    "typescript": "^5.9.3",
    "vite": "^8.0.9"
  }
}
