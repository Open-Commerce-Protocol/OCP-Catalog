# 进度快照 — 2026-05-27

## 当前在做的事

修复 **Coze Bot 第二轮测试失败** 引出的 tunnel URL 漂移问题,顺手把这个坑写进 `Coze上架手记.md`。

## 触发事件

Coze Bot 测试 `SONY WH-1000XM5 哪家最便宜` 时,Bot 内心独白显示:

> 「之前调用 skill_compare 失败了,错误是主机找不到」
> 「compare 和 search 都失败了」

不是 prompt / schema 问题,是公网入口挂了。

## 根因

`cloudflared` quick tunnel 的域名 `astrology-reserved-elizabeth-risks.trycloudflare.com` 已过期 / 解析失败。Coze 插件登记的还是旧 URL,所以每次调用都「主机找不到」。

`curl` 验证:
- 旧 URL `astrology-reserved-elizabeth-risks.trycloudflare.com` → `Could not resolve host` ❌
- 本地 `localhost:4330` → HTTP 200 ✅(gateway 本身没事)

## 已完成

- [x] 拉起新 tunnel:`https://its-task-welfare-newest.trycloudflare.com`(sjc10,通,200 OK 1.4s)
- [x] 确认旧 gateway 进程 PID = `480164`
- [x] Kill 旧 gateway + 用新 `SKILL_GATEWAY_PUBLIC_BASE_URL` 重启
  - 状态:gateway 已 listening on 4330(看到日志输出),但触发命令在后台 task 里 exit 127(疑似 `Stop-Process` 找不到 PID,但 gateway 启动成功)
  - **未验证**:`/openapi.yaml` 的 `servers.url` 是否已变成新 URL

## 待办(按顺序)

1. **验证 gateway 已经在用新 URL**
   - `curl https://its-task-welfare-newest.trycloudflare.com/openapi.yaml | grep "url:"` 应该显示新 URL
   - 如果还是旧 URL,说明重启时 env 没生效,需要再重启一次

2. **跑公网 smoke 13 项**
   ```bash
   SKILL_GATEWAY_SMOKE_KEY=sk_dev_demo_001 \
     SKILL_GATEWAY_SMOKE_BASE=https://its-task-welfare-newest.trycloudflare.com \
     bun run smoke:skill-gateway
   ```

3. **改 `Coze上架手记.md`**
   - 全文替换 `astrology-reserved-elizabeth-risks` → `its-task-welfare-newest`
   - 操作日期改成 2026-05-27
   - 新增踩坑条目:`Q6: quick tunnel URL 会漂吗?` —— 答:cloudflared 进程重启就换域名,Coze 插件登记的 URL 会失效。临时方案:重启 cloudflared → 拿新 URL → 重启 gateway(env 变了) → Coze 改插件 URL → **重新发布插件**(关键)。长期方案:升级到 cloudflare named tunnel + 自有域名。

4. **用户侧 Coze 改配置**
   - 插件 → 编辑 → 服务地址改成 `https://its-task-welfare-newest.trycloudflare.com` → 保存
   - **必须重新发布插件**,Bot 才能取到新配置
   - Bot 里清空对话(扫帚图标),测两轮:
     - `SONY WH-1000XM5 哪家最便宜` → 验 `skill_compare`
     - `推荐 200 元以内的充电器` → 验 `skill_recommend`

## 其它 Bot 改进建议(等当前修完再说)

人设里这句:

```
你用 ocp_search 搜索,然后用 ocp_deeplink 给出可点击的购买链接。
```

只点了 search + deeplink 两个工具,LLM 看到「比价」「预算 200 内」这类关键词时不敢主动用 compare / recommend。建议改成:

```
你是一个购物推荐助手。根据用户意图选择工具:
- 找商品 → skill_search,然后用 skill_deeplink 给购买链接
- 比价 / 「哪家最便宜」 → skill_compare
- 给预算 / 类目推荐 → skill_recommend
```

## 暂停项(优先级低,先放着)

- ChatGPT Custom GPT Actions 试上架(README Week 2 最后一个 unchecked)
- `scripts/mcp-skill-smoke.ts`
- 论文 4-2 公式格式 / 论文校对(用户明确说「先不管论文」)
