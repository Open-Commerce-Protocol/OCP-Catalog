{
  "name": "@ocp-catalog/ocp-activity-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "cd ../.. && bun --watch apps/ocp-activity-api/src/index.ts",
    "start": "cd ../.. && bun apps/ocp-activity-api/src/index.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "bun test --pass-with-no-tests"
  },
  "dependencies": {
    "@elysiajs/cors": "^1.3.3",
    "@ocp-catalog/auth-core": "workspace:*",
    "@ocp-catalog/activity-db": "workspace:*",
    "@ocp-catalog/config": "workspace:*",
    "@ocp-catalog/ocp-activity-core": "workspace:*",
    "@ocp-catalog/shared": "workspace:*",
    "elysia": "^1.3.22",
    "zod": "^4.1.12"
  }
}
